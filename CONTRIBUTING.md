# Contributing

Not yet open for external contributions while the first release is being
cut. Issues are welcome. Changes to `protocol/` require a matching change
to `conformance/` and a note in `protocol/CHANGELOG.md`.
