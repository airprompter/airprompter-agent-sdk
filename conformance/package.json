{
  "name": "@airprompter/protocol-conformance",
  "private": true,
  "version": "0.1.0",
  "description": "Runs protocol/schemas, protocol/examples and protocol/vectors against the reference implementations; every SDK runs the same vectors.",
  "type": "module",
  "engines": { "node": ">=20" },
  "scripts": {
    "test": "node run.mjs",
    "lint:openapi": "redocly lint ../protocol/openapi.yaml"
  },
  "devDependencies": {
    "@redocly/cli": "1.34.3",
    "ajv": "8.17.1",
    "ajv-formats": "3.0.1"
  }
}
