# Deploy manifests for the daemon

Planned: `systemd/airprompterd.service`, `launchd/com.airprompter.agentd.plist`,
a `windows/` service wrapper, a `docker/` sidecar image with a compose
example, and `kubernetes/daemonset.yaml`.
